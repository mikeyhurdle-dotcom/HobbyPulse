# Content Pipeline Audit — 2026-04-30

Run by: Claude Code (mikey.gilson@tealium.com sandbox)
Auditor model: Opus 4.7 (1M context)
Audit window: 2026-04-30 ~09:25 BST, ~15 min depth pass
Repos: `~/StormlightArchive/Projects/Personal/HobbyPulse` + tightbeams in `~/.../Tycho/tightbeam/`

---

## Executive summary (TL;DR)

1. **Hawk's blog-draft pipeline has only produced 2 PRs in the last 14 days (2 the prior 4 days, then nothing).** Both merged at 100% rate but median time-to-merge is ~73h. The 2 most recent Hawk runs each shipped 2 posts in a single PR — fine — but no Hawk PRs at all in the last 7 days. Either the cron is silent-failing or it's still paused from the 2026-04-27 Codex-quota incident (tightbeam `2026-04-30T06-33` confirms 6 Hawk crons were disabled and never explicitly re-enabled).

2. **Sitemap on both sites is broken: zero `/blog/*` URLs, and `/live` is still listed.** Google sees none of the 8 published blog posts on either site. `app/sitemap.ts` only emits `/boardgames/*` filesystem articles, never the per-vertical `/blog/*` posts. `/live` is still in `staticRoutes` even though PR #6 retires the route. **This is silently destroying SEO for all blog content on both sites.**

3. **Canonical URL bug across most non-`/deals` pages (live in prod).** The blog index, every blog post detail, `/boardgames`, `/boardgames/news`, and `/boardgames/best/*` all canonicalize to the root domain (`https://tabletopwatch.com` / `https://simracewatch.com`) instead of their own URL. Google will dedupe everything to homepage. This single bug + the missing sitemap entries means the entire editorial layer is invisible to search.

4. **35 boardgames/* AI-template articles sit half-published in repo.** They were generated by `scripts/boardgame-generate-drafts.mjs` (a non-LLM template filler producing literal "Game A / Pick #1" placeholders, generic Amazon search URLs, and links to disabled Wayland Games). All 28 have `draft: true`, so they're correctly hidden — but `/boardgames/versus` shows ZERO live articles because all 6 versus posts are draft. The 4 per-type sections show only 2-3 articles each despite 35 files on disk. Also: `config/boardgame-article-queue.json` is missing, so re-running the script will fail.

5. **4 Hawk-shipped blog posts merged into main with `draft: true` and never had the flag flipped.** They live in `content/blog/{tabletop,simracing}` but never reach readers. That's 50% of Hawk's actual content output silently un-published. No process catches this.

6. **Hawk has zero performance-feedback loop.** No GA4, Search Console, or analytics ingest in any tooling. `lib/analytics.ts` is a thin gtag wrapper that no-ops and includes a dead `trackBuildArmy` reference. Tealium iQ is wired (`utag.js` loads on both sites) but no vendors are mapped behind it yet (per tightbeam 2026-04-30T06-33). Hawk cannot adjust priorities based on real reader behaviour.

---

## Health dashboard

| Area | Status | Detail |
|---|---|---|
| Hawk PRs (TTW) | 🟡 | 1 PR with 2 posts in 14d (PR #2, 2026-04-21). 0 in last 7d. Merge rate 100%, t2m ~73h. |
| Hawk PRs (SRW) | 🟡 | 1 PR with 2 posts in 14d (PR #2). 0 in last 7d. Same merge stats. |
| Site population (TTW) | 🟡 | 4 visible blog posts (3 about Warhammer, 1 generic deals). 3/17 best-of, 0/6 versus, 2/7 how-to-play, 2/5 reviews of boardgames articles visible — rest are draft AI-slop. |
| Site population (SRW) | 🟡 | 4 visible blog posts. 1 marked deals, 3 evergreen. No SRW-equivalent of `/boardgames` content tree. Watch + Deals populated. |
| Sitemap quality | 🔴 | TTW: 781 URLs, ZERO `/blog/*`. SRW: 733 URLs, ZERO `/blog/*`. Both still list `/live`. |
| Canonicals | 🔴 | `/blog`, `/blog/<slug>`, `/boardgames`, `/boardgames/news` all canonicalize to `/` on both sites. |
| Structured data | 🔴 | Homepages have 2 JSON-LD scripts each. Blog post detail pages have **zero** schema.org markup — voice guide explicitly requires `BlogPosting`. |
| Affiliate compliance | 🟡 | `rel="sponsored"` absent on the deals blog post. eBay rover links rendered as bare `<a href>`. Amazon ASIN tag wiring exists in `lib/affiliate.ts` but not used in the deals blog markdown. |
| Voice / banned phrases | 🟢 | None of the voice-guide banned phrases ("In today's fast-paced…", "Whether you're a seasoned veteran…", "exciting world of") found in any post. Hawk-shipped posts read enthusiast-level. The `boardgames/*` template posts are AI-slop in a different way (placeholders, not banned phrases). |
| Vercel crons | 🟡 | 17 active per `vercel.json` (kickstarter intentionally on Hawk, not Vercel). All 11 endpoint dirs exist. **`live` cron route still on disk + in vercel.json** despite PR #6 staged to retire it (PR #6 still open). |
| SimRaceWatch parity | 🟡 | UNVERIFIABLE from this sandbox (no SSH/Supabase/Linear access). Tightbeam 2026-04-30T06-33 confirms 4 SRW crons added Phase 0 + 6 paused on 2026-04-27, never explicitly re-enabled. **Last Hawk SRW PR = 2026-04-21. Functional but stalled.** |
| Listings ingestion | 🟢 | Per tightbeam: TTW 22,686, SRW 8,810. Healthy. |
| Videos ingestion | 🟢 | Per tightbeam: 130 TTW + 38 SRW in last 14d. Sitemap has hundreds of /watch/* URLs. |
| Kickstarter tracker | 🟢 | 24 visible projects on `/kickstarter` (vs 30 scraped). Functional, freshly-shipped feature. |
| BGG board_games table | 🔴 | Empty (PULSE-4 / known blocker). XML API sunset; JSON migration parked. Blocks `/boardgames/games/[slug]` pages. |
| Robots.txt | 🟢 | Valid on both. `Disallow: /api/, /admin/`. Sitemap pointer present. |
| Dead code references | 🟡 | `analytics.ts.trackBuildArmy()` survives /build deletion. Sitemap has `/live`. `STALE_THRESHOLDS` in `bot-heartbeat` still includes `live`. |
| Hawk feedback loop | 🔴 | No GA4 / Search Console integration in any Hawk tooling. Cannot read what's working. |

---

## Findings (per audit area, with evidence)

### 1. Hawk content generation

**Stats (from GitHub API, last 100 PRs):**
- Total PRs in repo (all-time visible): 7. All authored by `mikeyhurdle-dotcom` (Hawk pushes via the repo owner's deploy key, not as a separate bot user — so attribution must use branch prefix).
- Branch-prefix distribution: `mikeyhurdle/*` 3, `hawk/*` 2, `mikey/*` 1, `tycho/*` 1.
- **Hawk PRs in last 14d: 2** (#1 + #2, both on branch `hawk/blog-2026-04-16`)
- **Hawk PRs in last 7d: 0**
- **Hawk PRs in last 30d: 2** (same as 14d — only opened/merged in mid-April)
- Hawk merge rate: 2/2 = **100%**
- Hawk median time-to-merge: **~73 hours** (3 days)
- Stale (open >7d) PRs: **0**

**PR titles seen:**
- #1 (2026-04-21): "Draft blog posts: 40k detachment buys + iRacing S2 track buy order" — branch `hawk/blog-2026-04-16`
- #2 (2026-04-23): "Draft blogs: Armageddon detachment buying guide + MOZA/SIMAGIC upgrade timing" — branch `hawk/blog-2026-04-16`

**Currently open PRs (all human-authored):**
- #3 (1d): `feat(tycho): instrument all 17 Vercel crons + add off-VPS watchdog` — `tycho/heartbeat-instrumentation`
- #6 (0d): `chore(live): kill live cron + retire /live page (PULSE-2)` — staged but unmerged
- #7 (0d): `PULSE-47: Retire phase 2 of board-game-content cron + drop orphan articles table`

**Topic mix (last 14d):**
- Of 4 Hawk-shipped posts, 3 are Warhammer/40K-detachment-focused (one specifically the "Armageddon" one). Despite the 2026-04-27 board-game-first pivot, **none of the recent Hawk posts target board games**. The TTW content queue (`bot/CONTENT-QUEUE.md`) lists 5 priority titles (Best 2-player, Wingspan alternatives, Best gateway, KS roundup, Catan vs Carcassonne) and ALL are unstarted (`[ ]` × 5).
- The pivot didn't reach Hawk's actual output. Hawk is still drafting 40K-edition-change content — likely because the Phase 0 crons added on the VPS were SRW-parity ones, not anti-warhammer guardrails on the TTW cron.

**Issues:**
- **Both AM (08:45) and PM (17:30) drafters not firing for last 7 days.** Could be: (a) still paused from the 2026-04-27 Codex-quota incident; (b) cron-side script error; (c) only failing the "open PR" step. Unverifiable without VPS/cron-log access — confirmed in the "Questions for Mikey" section.
- **Topic drift away from pivot.** Hawk is still drafting Warhammer-edition content despite the explicit `bot/CONTENT-QUEUE.md` board-game list.
- **Hawk PRs bundle 2 posts into 1 PR** (one TTW + one SRW). This makes per-site merge attribution impossible from PR title alone.

**Recommendations:**
- Verify on VPS: `ssh root@<vps>; sudo -u openclaw crontab -l | grep hawk-bg-draft`. Check `/root/.openclaw/logs/hawk-bg-draft-generator-am-*.log` for the last successful run timestamp.
- Have Hawk separate per-site PRs so `gh pr list` filtering by branch works (e.g. `hawk/blog-ttw-<date>` vs `hawk/blog-srw-<date>`).
- Force a one-shot Hawk run to clear queue items 1–5 from `bot/CONTENT-QUEUE.md` (Best 2-player, Wingspan alternatives, etc.) explicitly NAMED in the cron prompt so it can't pick anything else.

### 2. SimRaceWatch parity (Phase 0 crons)

**Status: UNVERIFIABLE from this sandbox.**

I cannot SSH the VPS, list crontab, or read `~/.openclaw/logs/hawk-srw-*.log` from here. Tightbeam evidence:
- 2026-04-28T16:30Z (post-Phase-0 commit `3931a93`): "Four SimRaceWatch parity crons added on the VPS (content-queue, draft-am, deals-roundup, weekly-planner) with proper offsets so they don't fight tabletop crons for Codex tokens."
- 2026-04-30T06-33 (in-flight): "6 heavy pulsebot crons disabled" — including `hawk-srw-content-queue` and `hawk-srw-draft-generator-am`. **Re-enable was deferred to "after 18:00 BST today when Codex Pro daily quota resets"** — that was 2026-04-27. No subsequent tightbeam confirms re-enable.

Indirect evidence: Hawk shipped a SRW post on 2026-04-21 (PR #1: "iRacing S2 track buy order") and another on 2026-04-23 (PR #2: "MOZA/SIMAGIC upgrade timing"). Neither is freshly board-game-pivot work; SRW didn't pivot. So the SRW draft-AM cron was firing as recently as 2026-04-23, but no SRW PR since. Same 7-day silence as TTW. **Probable cause: 2026-04-27 pause never lifted.**

**Recommendation:** Mikey to manually re-enable on VPS via the one-liner captured in `feedback_rebrand_day_load.md` (per tightbeam). Then watch for 24h whether AM/PM drafters fire on schedule.

### 3. Live site content health

**Live URLs hit and their status:**

| URL | HTTP | Visible content |
|---|---|---|
| `https://tabletopwatch.com/` | 200 | Full home — KS strip, board game articles row, miniatures section. |
| `https://tabletopwatch.com/blog` | 200 | 4 posts. Should be 6 — 2 hidden by `draft:true`. |
| `https://tabletopwatch.com/boardgames` | 200 | Populated landing. |
| `https://tabletopwatch.com/boardgames/news` | 200 | H1 only. **No articles, no blog posts linked.** Empty. |
| `https://tabletopwatch.com/boardgames/best` | 200 | 3 visible articles (vs 17 on disk). |
| `https://tabletopwatch.com/boardgames/versus` | 200 | **ZERO visible articles** (vs 6 on disk). All draft. |
| `https://tabletopwatch.com/boardgames/how-to-play` | 200 | 2 visible (vs 7 on disk). |
| `https://tabletopwatch.com/boardgames/reviews` | 200 | 2 visible (vs 5 on disk). |
| `https://tabletopwatch.com/kickstarter` | 200 | 24 KS projects visible. |
| `https://tabletopwatch.com/deals` | 200 | Populated, real listings. |
| `https://simracewatch.com/` | 200 | Home — replays ("Dropping Right Now", "Latest Replays", "Tracked Channels"). **No blog/articles section.** |
| `https://simracewatch.com/blog` | 200 | 4 posts. Should be 6 — 2 hidden by `draft:true`. |
| `https://simracewatch.com/deals` | 200 | Populated, real listings. |
| `https://simracewatch.com/watch` | 200 | Populated. |

**Blog post visibility audit (count of files vs visible):**

```
content/blog/tabletop/        files=6  visible=4   hidden:
  - armageddon-detachments-2026-what-to-buy-and-what-to-skip.md (draft:true) ← Hawk PR #2
  - warhammer-40k-11th-edition-detachment-changes-what-to-buy-first.md (draft:true) ← Hawk PR #1

content/blog/simracing/       files=6  visible=4   hidden:
  - moza-vs-simagic-2026-upgrade-timing-guide.md (draft:true) ← Hawk PR #2
  - iracing-track-list-2026-season-2-rookie-priority-buy-order.md (draft:true) ← Hawk PR #1
```

→ **All 4 hidden posts came from the 2 merged Hawk PRs.** Hawk drafts with `draft: true` (correct, safe-by-default), but the merge ritual isn't flipping them to `draft: false`. Half of Hawk's output is invisible to readers AND to search.

**TTW homepage links to `/blog/*`:** zero. The "Latest Board Game Articles" rail points only to `/boardgames/<type>/<slug>` URLs. Hawk's `/blog/*` editorial is not surfaced from the homepage at all on either site.

**SRW homepage:** No editorial / blog feed at all (only Replays + Channels). The 4 visible SRW blog posts (live at `/blog/<slug>`) have zero on-site discoverability paths from `/`.

### 4. Data-ingestion pipeline

Cannot query Supabase from sandbox (`mcp__claude_ai_Supabase__list_projects` blocked). Pulled from tightbeam `2026-04-30T06-26-tabletopwatch.md` (most recent product-stat snapshot):

| Metric | Value | Trend |
|---|---|---|
| Tabletop listings | 22,686 | Healthy, growing per tightbeam |
| Sim racing listings | 8,810 | Healthy, growing |
| TTW videos last 14d | 130 | Healthy |
| SRW videos last 14d | 38 | Healthy |
| Active live streams | 95 (when /live was alive) | About to be retired — PR #6 |
| Kickstarter projects | 30 scraped → 24 visible | Healthy, fresh |
| `board_games` table | 0 rows | 🔴 BLOCKED on BGG XML→JSON migration (60–90 min job) |
| `live_streams` table | 10,529 rows (archived) | Decision pending: drop/archive/keep |

**Hawk content-queue depth (`bot/CONTENT-QUEUE.md`):** 5 priority titles, ALL `[ ]` unstarted. Queue is full but not draining. AM/PM drafters appear paused (see §1).

### 5. Cron health post-PULSE-2 / PULSE-47

Active in `vercel.json` (17):
- `youtube` (06:00 daily)
- `parse` (07:00 daily)
- `transcripts` (07:30 daily)
- `deals?batch=0..8` (02:00–02:24 daily, 9 separate entries)
- `live` (12:00 daily) ← 🔴 should be deleted post PR #6 merge
- `price-alerts` (08:00 + 18:00 twice daily)
- `discover` (08:00 Sunday weekly)
- `board-game-transcripts` (06:15 daily)

**Notable absences:**
- No `kickstarter` Vercel cron (intentional per Phase 4 incident — Hawk owns it)
- No `board-game-content` Vercel cron in this `vercel.json` snapshot — the PULSE-47 retiral PR (#7) is in flight to remove its Phase 2 dead code

**Cross-reference vs tightbeam Tycho heartbeat instrumentation:**
- Tightbeam 2026-04-30T08-43 mentions PR #3 (`feat(tycho): instrument all 17 Vercel crons + add off-VPS watchdog`) is open. Tycho can detect silent cron failures — but the watchdog is unmerged.
- Cannot reach Tycho dashboard from this sandbox to confirm which crons are actually firing.

**Silent-failure suspects:**
- `board-game-transcripts` 06:15 — feeds `board_game_videos.transcript`. Per PULSE-3 audit (tightbeam 2026-04-30T09-00), Phase 1 (transcripts) is still useful and presumed working. Unverified count of transcripts ingested last 14d.
- `discover` weekly — purpose: discover new YouTube channels. No tightbeam mention this week. Last fired? Unknown.

### 6. Voice + tone audit (lighter pass)

**Hawk-shipped blog posts (samples):**
- `content/blog/tabletop/this-weeks-best-deals-warhammer-board-games-april-24-2026.md` — 7,870 chars. Reads enthusiast, real numbers, real eBay listings, mostly compliant with voice guide. Has affiliate hooks (rover.ebay.com, goblingaming.co.uk). Personality clear.
- `content/blog/simracing/best-sim-racing-upgrades-under-200.md` — 7,115 chars. Strong "Upgrade Coach" persona, ROI framing, Best for / Skip if blocks present. Generic Amazon search URLs (`amazon.co.uk/s?k=...`) — these aren't ASIN-specific affiliate links. Compliant.
- None of the 6 Warhammer posts trip the banned-phrase filter.

**`content/boardgames/*` template posts (sample):**
- `boardgames/best/best-2-player-board-games-for-couples.md` — 2,208 chars. Literal placeholders: "Best 2-Player Board Games for Couples Pick #1", "Pick #2", "Pick #3". Generic Amazon search URL. Wayland Games URL despite Wayland being **disabled** (CLAUDE.md). Frontmatter `amazonAsin: "B0DUMMY368"` — placeholder ASIN.
- `boardgames/versus/azul-vs-sagrada.md` — 1,928 chars. Literal "Game A" and "Game B" in the body. No actual mention of Azul or Sagrada beyond the title.
- `boardgames/best/best-board-games-christmas-2026.md` — 819 chars. Literal "*Content to be generated by the article pipeline.*" stub.

**Source: `scripts/boardgame-generate-drafts.mjs`.** This is a **non-LLM template filler**, not an LLM call. It stamps frontmatter + identical body sections per article-type into files, defaults `draft: true` (good), writes generic affiliate URLs, and references Wayland Games which is disabled. Looks like it was a Phase 5 stub Mikey wrote pre-pivot to bulk-create content shells, intended to be filled in by Hawk later. Hawk hasn't filled them in. **They are SEO landmines if anyone flips draft:false without a rewrite.**

Also: `config/boardgame-article-queue.json` — referenced by the script — **does not exist in the repo**. The script will throw on next run. Either deleted in cleanup, never committed, or victim of a git revert.

### 7. SEO basics

**Robots.txt — 🟢 healthy on both:**
```
User-Agent: *
Allow: /
Disallow: /api/
Disallow: /admin/

Sitemap: https://<domain>/sitemap.xml
```

**Sitemap.xml — 🔴 broken in two ways:**

| Site | Total `<url>` | `/blog/*` URLs | `/live` listed? | Comment |
|---|---|---|---|---|
| tabletopwatch.com | 781 | **0** | yes 🔴 | Has 5 boardgames articles, hundreds of watch/* + deals/* + miniatures channels. Good breadth, missing the editorial layer entirely. |
| simracewatch.com | 733 | **0** | yes 🔴 | Lots of /watch/* and /deals/c/*. No editorial layer. |

Source: `app/sitemap.ts`. Lines 50–60 emit `/boardgames/{type}/<slug>` for filesystem articles, but `/blog/*` is **never iterated**. Filesystem helper `lib/blog.ts:listSlugs` exists; sitemap.ts simply doesn't call it. Line 29 still hard-codes `/live`.

**Canonical bug — 🔴 live in prod:**

| URL | Canonical |
|---|---|
| `https://tabletopwatch.com/blog` | `https://tabletopwatch.com` ❌ |
| `https://tabletopwatch.com/blog/this-weeks-best-deals-...` | `https://tabletopwatch.com` ❌ |
| `https://tabletopwatch.com/boardgames` | `https://tabletopwatch.com` ❌ |
| `https://tabletopwatch.com/boardgames/news` | `https://tabletopwatch.com` ❌ |
| `https://tabletopwatch.com/deals` | `https://tabletopwatch.com/deals` ✅ |
| `https://tabletopwatch.com/kickstarter` | `https://tabletopwatch.com/kickstarter` ✅ |
| `https://simracewatch.com/blog` | `https://simracewatch.com` ❌ |

Source: `app/(vertical)/blog/[slug]/page.tsx` `generateMetadata` doesn't set `alternates: { canonical: ... }`. The root layout default canonical is winning. Same for `/boardgames` listing pages.

**Structured data — 🔴 missing on blog posts:**
- TTW + SRW homepages: 2 JSON-LD blocks each (likely WebSite + Organization).
- Blog post detail pages: **zero** JSON-LD. Voice guide section 7 says schema.org `BlogPosting` is required.

**Internal linking on Hawk posts:** Spot-check `this-weeks-best-deals-warhammer-board-games-april-24-2026.md` — links externally to retailers + eBay rover, no internal links to `/deals`, `/kickstarter`, `/boardgames`. Voice guide mandates them.

**`rel="sponsored"` on affiliate links — 🔴 missing.** Zero hits on the deals blog post HTML for `rel="sponsored"`. eBay rover URLs and Amazon URLs are bare anchors. This:
- Breaks Tealium iQ delegate-listener wiring (per tightbeam: "Affiliate-click delegation fires `utag.link({ tealium_event: "affiliate_click", … })` for every anchor with `rel*="sponsored"`"). With no sponsored attribute, NO clicks are tracked.
- Risks Google manual action for unmarked affiliate/sponsored content per Google's webmaster guidelines.

**Meta titles & descriptions:** Present and sensible on every page tested. The blog index has the doubled-brand bug `Blog | TabletopWatch | Tabletop Watch — Board Games & Miniatures` (siteName concatenated twice via metadata template).

### 8. Surfacing question — feedback loop

**Hawk has no analytics access today.**

Searched: `lib`, `app`, `scripts`, `bot` for `GA4|google-analytics|gtag(|searchconsole|search-console|webmasters|measurement_id|MEASUREMENT_ID`.

Hits:
- `lib/analytics.ts` — thin gtag wrapper, no-ops if `window.gtag` missing. Includes `trackBuildArmy` which is dead post-/build-deletion.
- `lib/tealium.ts` — utag loader (per tightbeam: wired, but no GA4 vendor mapped behind it yet).
- `app/api/bot-heartbeat/route.ts` — references `NEXT_PUBLIC_GA4_ID` env var as a "warn if missing" check only. No actual GA4 read.

Hawk's tooling chest (`bot/TOOLS.md`):
- `browse.py` (web pages), `supabase_query.py`, `github_api.py`, `vercel_check.py`, `vault_edit.py`, `linear_task.py`, `send_email.py`
- **No GA4 client, no Search Console client.** Hawk cannot read what's working.

This is the single biggest blocker for Hawk becoming a self-tuning content engine. Until Hawk can see (a) GA4 sessions / page_view / outbound_click and (b) Search Console queries+impressions+CTR, the content queue is just Mikey's prior hypothesis frozen in time.

---

## Silent failures / drift

Things broken right now that nobody's noticing:

1. **Sitemap missing `/blog/*` on both sites.** Google Search Console will not see those URLs unless it stumbles on them via internal links — which the homepage doesn't have for blog posts. Estimated SEO impact: blog posts are de facto unindexed.

2. **Canonical bug routes most page indexing back to `/`.** Even if Google did stumble on `/blog/this-weeks-best-deals-...`, the canonical instructs Google to dedupe to `/`. Compounds with #1.

3. **Half of Hawk's blog posts merged with `draft: true` and never flipped.** Hawk PR #1 (40K detachment + iRacing) and PR #2 (Armageddon + MOZA/SIMAGIC) — 4 of 4 posts merged with `draft: true`. Reader-invisible. The merge ritual is missing a "flip draft flag" step.

4. **`/live` is still in `vercel.json` cron list and `app/sitemap.ts` static routes** despite PR #6 retiring the route. Will start 404'ing Google + 404'ing the cron once #6 lands.

5. **Hawk drafting Warhammer content despite the board-game-first pivot.** All 4 of Hawk's TTW posts in last 14d are 40K-edition-change content; none address the 5 unstarted titles in `bot/CONTENT-QUEUE.md`.

6. **`config/boardgame-article-queue.json` deleted/missing.** `scripts/boardgame-generate-drafts.mjs` reads it on line 6. Next run = `Error: ENOENT`.

7. **`rel="sponsored"` absent on affiliate links.** Tealium `utag.link({ tealium_event: "affiliate_click", … })` delegate listener is matching on `rel*="sponsored"` per the wired-up config. With zero sponsored anchors, **Tealium currently tracks zero affiliate clicks site-wide.** This is invisible until Mikey wires the GA4 vendor and notices the count is zero.

8. **`analytics.ts.trackBuildArmy()` survives /build deletion.** Dead code; will silently no-op.

9. **`STALE_THRESHOLDS["live"]` in `app/api/bot-heartbeat/route.ts`** still expects a 30-min freshness on a task that's about to be deleted. Will trigger spurious "stale" warnings from PulseBot self-checks once `live` cron is gone.

10. **AdSense `<script>` loads on every page** (`pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6359087037553084`) per audit of homepage HTML — but I see no `<ins class="adsbygoogle">` slots in the sampled pages. Either ads aren't configured to render anywhere yet, or AdSense is still under review (matches MEMORY.md "AdSense under Google review"). Auto-ads MAY be on; can't tell without DOM render. Worth verifying nothing's banned for "blank ad units".

---

## Recommendations (ranked by impact)

### P1 — Do this week, biggest revenue/SEO impact

1. **Fix the canonical bug + add `/blog/*` to sitemap.** ~30 min PR. In `app/sitemap.ts`, after the boardgames loop, add a per-vertical loop over `lib/blog.ts:listSlugs(vertical.slug)`. Push `${baseUrl}/blog/<slug>` entries. In `app/(vertical)/blog/[slug]/page.tsx` `generateMetadata`, set `alternates: { canonical: \`https://${brand.domain}/blog/${slug}\` }`. Same for `/boardgames` listing/detail pages. **One PR unblocks ALL of Hawk's editorial output for SEO indexing.**

2. **Flip draft flag on the 4 Hawk-shipped blog posts that landed as drafts.** Manual edit, 5 min. The 4 files are listed in §3. Either flip them now (if the content is good — quick read says yes) or keep them draft and fix the merge ritual to flip explicitly going forward.

3. **Re-enable the 6 paused Hawk crons on the VPS.** One-line ssh per tightbeam `feedback_rebrand_day_load.md`. Then watch for 24h whether AM/PM drafters fire. If not — debug Hawk's session, not the cron schedule.

4. **Add `rel="sponsored" target="_blank"` to all affiliate anchors.** Without this, Tealium tracks zero clicks. Two-pronged: (a) audit the markdown blog template to render affiliate links with `rel="sponsored"` always (the markdown renderer in `app/(vertical)/blog/[slug]/page.tsx` is `react-markdown` — set a custom anchor renderer that inspects href and adds rel/target for retailer hosts); (b) fix the same in the deals component for product cards.

### P2 — Do this month, structural

5. **Update `bot/CONTENT-QUEUE.md` to actually drive Hawk's prompt.** The cron that picks queue items needs to NAME them in the prompt: e.g. "Pick the top unworked title from `bot/CONTENT-QUEUE.md` lines starting with `[ ]`. The current top is 'Best 2-player board games of 2026'." Otherwise Hawk free-associates and ships warhammer content.

6. **Decide on the 35 boardgames/* AI-template articles.** Three options: (a) **delete** them all and treat `boardgames/*` as Hawk-only output going forward; (b) **rewrite** the highest-value ~5 (e.g. wingspan, azul-vs-sagrada, gateway picks) using Hawk in a single drafting session; (c) **leave drafts** and ignore — but delete the script + queue.json reference so they can't regenerate. **Recommend (a) — delete the AI-slop entirely. They're dead weight + a future SEO landmine.**

7. **Commit PR #6 (PULSE-2) — it's just sitting open.** Removing `/live` from vercel.json + sitemap.ts + heartbeat thresholds is a clean win. After merge, do a 30-second sweep for the 3 remaining `/live` references in code: `analytics.ts`, `bot-heartbeat/route.ts`, `app/sitemap.ts`.

8. **Add JSON-LD `BlogPosting` schema to `app/(vertical)/blog/[slug]/page.tsx`.** ~15 min. Voice guide requires it.

9. **Surface the editorial layer on the homepage.** Both sites: add a "Latest from the blog" rail with the top 3 `/blog/*` posts. SRW homepage in particular has zero editorial discoverability.

### P3 — Quarterly, foundational

10. **Build the Hawk feedback loop.** Two stages:
    - **Stage 1 (~half day):** wire Search Console API into a new `bot/tools/search_console.py` script that pulls 28-day query/page/CTR data into a Supabase table. Hawk reads it from a daily cron and bumps `bot/CONTENT-QUEUE.md` priorities based on impression-without-clicks (high-intent gap pages) and impression-with-poor-CTR (rewrite candidates).
    - **Stage 2 (~half day):** wire GA4 Data API for outbound_click events tagged by retailer. Lets Hawk see which affiliate links actually convert and weight new posts toward winners.
    - This is the "Hawk feedback loop" question Mikey has been deferring. Until this exists, Hawk is flying blind.

11. **Migrate BGG XML → JSON ingest.** 60–90 min job per the 2026-04-28 tightbeam. Unblocks `/boardgames/games/[slug]` pages (currently empty `board_games` table). Also unblocks `<PriceComparisonStrip>` on game detail pages (Phase 5 work that landed but has no data).

12. **Per-site PR branches for Hawk** so attribution is per-site (`hawk/blog-ttw-<date>` vs `hawk/blog-srw-<date>`). Trivial change in Hawk's drafter prompt.

---

## Questions for Mikey

Things I couldn't verify from this sandbox — needs Mikey to run:

1. **Are the AM/PM drafter crons actually firing right now?** Run on the VPS:
   ```bash
   sudo -u openclaw crontab -l | grep -E 'hawk-(bg|srw)-draft-generator'
   tail -50 /root/.openclaw/logs/hawk-bg-draft-generator-am.log
   tail -50 /root/.openclaw/logs/hawk-srw-draft-generator-am.log
   ```
   If empty / older than 7d → re-enable per `feedback_rebrand_day_load.md`.

2. **Was the post-Codex-quota cron pause ever lifted?** Tightbeam 2026-04-28 said "Mikey: re-enable Hawk's 6 paused crons after 18:00 BST today". I don't see a confirming tightbeam in the last 48h. Status?

3. **Tycho dashboard cron status (cross-reference).** Is PR #3 (`feat(tycho): instrument all 17 Vercel crons + add off-VPS watchdog`) merged? If yes — what does Tycho say is dead vs alive? Dashboard URL?

4. **Should we delete the 35 `content/boardgames/*` AI-template files entirely?** Or is the plan still to have Hawk uplift them in-place? Recommending delete unless there's a strategy here.

5. **`config/boardgame-article-queue.json` is missing.** Was this an intentional cleanup, or an accidental git rm? Determines whether to keep the script.

6. **AdSense status — under review or approved?** Auto-ads loading site-wide with no `<ins>` slots could be either. Worth a Google AdSense console check.

7. **Hawk feedback-loop ticket placement.** Per tightbeam 2026-04-30T08-43: "Decide whether the Hawk feedback loop (read GA4/Search Console → adjust `bot/CONTENT-QUEUE.md` priorities) lives as a 5th routine inside PULSE-48 or a separate new ticket." Recommendation: separate ticket — it's a half-day build, not a 30-min routine. Want me to open it next session?

8. **Are the 4 Warhammer/sim-racing posts that Hawk shipped good enough to flip from draft to live?** Quick read says yes on at least 3. Want me to do a content review pass and flip the draft flags in a follow-up PR?

---

## Methodology / what I checked

**Files read:**
- `bot/REFERENCE.md`, `IDENTITY.md`, `SOUL.md`, `HEARTBEAT.md`, `CONTENT-QUEUE.md`, `MEMORY.md`, `TOOLS.md`
- `bot/sites/tabletopwatch.md`, `bot/sites/simracewatch.md`
- `vercel.json`, `CLAUDE.md`
- `app/sitemap.ts`, `app/api/bot-heartbeat/route.ts`, `app/(vertical)/blog/[slug]/page.tsx`
- `lib/blog.ts`, `lib/boardgame-articles.ts`, `lib/analytics.ts`
- `scripts/boardgame-generate-drafts.mjs`
- 7 recent tightbeams (2026-04-28 to 2026-04-30, all TTW + SRW + cross-site)
- All 12 blog post markdown files (`content/blog/{tabletop,simracing}/*.md`)
- Sample of `content/boardgames/{best,versus,reviews,how-to-play}/*.md` (3 read in full, all 35 listed)
- `.git/logs/HEAD` (last 126 reflog entries) for commit history

**Live URLs hit (curl):**
- TabletopWatch: `/`, `/blog`, `/blog/<slug>`, `/deals`, `/kickstarter`, `/boardgames`, `/boardgames/news`, `/boardgames/best`, `/boardgames/versus`, `/boardgames/how-to-play`, `/boardgames/reviews`, `/sitemap.xml`, `/robots.txt`
- SimRaceWatch: `/`, `/blog`, `/blog/<slug>`, `/deals`, `/watch`, `/sitemap.xml`, `/robots.txt`

**APIs hit:**
- `https://api.github.com/repos/mikeyhurdle-dotcom/HobbyPulse/pulls?state=all&per_page=100&sort=updated&direction=desc` — analyzed all 7 PRs in the response

**APIs / tools BLOCKED in this sandbox (gaps documented as such):**
- `gh` CLI (cannot use `gh pr list` / `gh api`)
- Linear MCP (cannot query PULSE-* tickets directly)
- Supabase MCP (cannot run `execute_sql` to verify table counts, listings deltas, transcript counts)
- WebFetch (cannot crawl the live Tycho dashboard)
- Direct git CLI (`git log`, `git status` blocked) — worked around via `.git/logs/HEAD` reflog read
- SSH to VPS (no key access from this sandbox)

So all VPS-side cron + log questions are deferred to Mikey in §Questions.

---

*Generated 2026-04-30 ~09:30 BST. ~15 min sandbox session.*
